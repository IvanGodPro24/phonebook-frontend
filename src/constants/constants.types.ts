export type StringOptions = {
  value: string;
  label: string;
};

export type NumberOptions = {
  value: number;
  label: string;
};

export type BooleanOptions = {
  value: boolean;
  label: string;
};
